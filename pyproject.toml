[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
check_untyped_defs = true
no_implicit_optional = true
files = ["analysis_engine.py", "consistency_engine.py", "scheduler.py", "state.py", "runtime.py"]

[tool.pytest.ini_options]
markers = [
  "staging: credentialed integration test; requires real Groq, Tavily, Notion credentials",
]
testpaths = ["tests"]
